# Leave blank to use the local mock dataset (src/mocks/users.json).
# Once you've created your mockapi.io project and uploaded the 500 generated
# records, paste its base URL here (e.g. https://<id>.mockapi.io/api/v1)
# and the app will fetch from it instead — no code changes needed.
VITE_API_BASE_URL=
